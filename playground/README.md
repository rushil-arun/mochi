## Playground

Files to mess around with SLMS.

For `smollm.py`, first run:

    pip install accelerate

    accelerate config

Follow the instructions to set up the config file. Then, run:

    python smollm.py



